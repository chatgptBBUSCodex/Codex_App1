# Static Image Placeholder

The hero background is embedded directly in the stylesheet as a base64-encoded data URI to avoid binary assets in the repository.

Add new images to this directory if you need to swap in alternative artwork and update the CSS accordingly.
