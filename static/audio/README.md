# Audio Placeholder

The hover audio cue for the hero title is provided via an inline base64-encoded data URI so that the repository does not contain binary media.

Place custom audio files in this directory if you would prefer to stream them from disk and update `templates/index.html` accordingly.
